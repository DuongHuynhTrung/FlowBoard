version: '3.8'
services:
  nginx:
    image: nginx:stable-alpine
    container_name: flowboard-nginx-prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/nginx.prod.conf:/etc/nginx/nginx.conf:ro
